"""CostGuard Split server API layer (P0-04/05/06).

OPTIONAL dependency surface: requires fastapi+pydantic (install via
`costguard-agent[split-server]`). The core costguard_split package stays
zero-dependency; nothing outside this subpackage imports fastapi.
"""
